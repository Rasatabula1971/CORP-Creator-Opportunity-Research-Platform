services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: corp
      POSTGRES_PASSWORD: corp
      POSTGRES_DB: corp
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
